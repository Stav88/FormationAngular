import { UserIdDirective } from './user-id.directive';

describe('UserIdDirective', () => {
  it('should create an instance', () => {
    const directive = new UserIdDirective();
    expect(directive).toBeTruthy();
  });
});
